import InputField from "@/shared/components/InputField";

const EmailInputField = () => {
  return (
    <InputField id="email" label="Email Address" placeholder="Email Address" />
  );
};

export default EmailInputField;
